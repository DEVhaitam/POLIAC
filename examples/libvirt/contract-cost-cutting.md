# Change objective

We are over budget this quarter. Cut infrastructure spend on this deployment.

- Primary goal: reduce compute cost for this VM.
- Some latency increase under peak load is acceptable — this deployment is not expected to
  serve peak-level concurrency as its normal operating condition.
- Do not cut capacity below what's needed to keep the system stable at everyday (low-to-medium
  concurrency) load.